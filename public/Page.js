import Component from "./Component.js";

class Page extends Component{
  constructor(parentElement){
    super(parentElement);
    this.element.generateHTML();
  }
  const inputText =
  
}

export default Page;